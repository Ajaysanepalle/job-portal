version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      # Persist DB outside the source mount so reloads/rebuilds don't wipe it.
      - DATABASE_URL=sqlite:////data/job_portal.db
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123
    volumes:
      - ./backend:/app
      - job_portal_data:/data
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    image: python:3.11-slim
    working_dir: /app/frontend
    ports:
      - "8080:8080"
    volumes:
      - ./frontend:/app/frontend
    command: python -m http.server 8080

  # Optional: Use Nginx as reverse proxy
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend
      - frontend

volumes:
  job_portal_data:
